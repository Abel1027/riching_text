name: riching_text
description: Build a rich text by providing a text and the words/phrases you want to highlight within.
version: 0.1.0
repository: https://github.com/Abel1027/riching_text

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
